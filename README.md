# Cris Team Test Skill Package

Small participant-style package for validating the upload/intake pipeline.

The package is intentionally simple:

- `pick-fantasy-xi` selects a valid 11-player roster from the provided board along with instructions for risk plays

The skills are Markdown instructions only. They should not require network access, scripts, credentials, or writing files.
